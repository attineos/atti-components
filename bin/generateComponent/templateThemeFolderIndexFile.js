export default name => `// Create the theme part for ${name}. Please add a call into the componentsFactory.
export default ({ border, colors, fonts, opacities, sizes, spaces }) => ({
  colors: {
    // Add properties here.
  },
  spaces: {
    // Add properties here.
  },
})
`
