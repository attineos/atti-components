export default ({ borders, colors }) => ({
  borders: {
    width: borders.widths.bw1,
  },
  colors: {
    border: 'currentcolor',
    borderHover: 'currentcolor',
    text: colors.secondary,
  },
})
