---
path: '/inputtime'
name: 'InputTime'
title: 'InputTime'
---

With the InputTime you can select a time which is composed of hours / minutes / seconds.

## Examples

Uncontrolled component:

```js
<InputTime onChange={date => console.log(date)} />
```

Controlled component:

```js
const Example = () => {
  const [date, setDate] = useState(new Date())
  return <InputTime value={date} onChange={date => setDate({ date })} />
}

render(<Example />)
```

## Theme

<Theme name="components.inputTime" />
