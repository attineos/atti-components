import styled from "styled-components"

const Pre = styled("pre")`
  margin-bottom: ${({ theme }) => theme.spaces.s1_5} !important;
  margin-top: ${({ theme }) => theme.spaces.s1_5} !important;
`

export default Pre
