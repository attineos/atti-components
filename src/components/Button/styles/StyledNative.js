import styled from 'styled-components'
// eslint-disable-next-line
import Button from 'react-native'

const StyledNative = styled.Button.attrs({
  title: 'password',
})``

export default StyledNative
