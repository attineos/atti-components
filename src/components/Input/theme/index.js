export default ({ colors, spaces }) => ({
  input: {
    colors: {
      text: colors.primary,
      background: colors.white,
    },
  },
  label: {
    spaces: {
      marginBottom: spaces.s4,
    },
  },
})
